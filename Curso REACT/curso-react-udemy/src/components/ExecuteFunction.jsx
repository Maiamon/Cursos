const ExecuteFunction = (props) => {
    return (
        <div>
            <button onClick={props.myFunction}>Clique aqui para executar a função</button>
        </div>
    )
}

export default ExecuteFunction